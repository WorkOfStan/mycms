# ignore third-party libraries, so that prettier doesn't change them or super-linter fails with CSS_PRETTIER, JAVASCRIPT_PRETTIER, JSON_PRETTIER, MARKDOWN_PRETTIER
#assets/uls/**
# scripts and styles contain third-party libraries along internal CSS and JS, so let's ignore it for time being
scripts/**
styles/**
