# These rules are applied only if in the root folder.

# ignore SASS files, as it seems that stylelint is evaluating them the same way as CSS files resulting in e.g.
# Expected single space before "{"                                                     block-opening-brace-space-before 
# Expected a trailing semicolon                                                        declaration-block-trailing-semicolon
dist/styles/*.sass
dist/styles/components/*.sass

# generated CSS
dist/styles/index.css

# 3rd party CSS
dist/scripts/lib/owlcarousel/assets/owl.*.css
dist/styles/bootstrap.css
dist/styles/bootstrap-datetimepicker.css
dist/styles/font-awesome.css
dist/styles/ie10-viewport-bug-workaround.css
dist/styles/summernote.css
