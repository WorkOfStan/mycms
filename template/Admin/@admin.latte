<!DOCTYPE html>
<html lang="{$language}">
{include '../inherite.latte', latte => 'Admin/inc-head.latte'}
    <body>
        <header>
            {include 'inc-navigation.latte'}
        </header>
        {*
        <div class="container-fluid row">
            {include 'admin-user-agendas.latte'}
        </div>
        *}
        {block htmlbody}{/block}
        {if $authUser}
            {$htmlDashboard|noescape} {* TODO refactor away from HTML *}
        {/if}
        </main></div> {* TODO move the start of these tags from htmlbody *}

        <!-- footer -->
        <footer class="sticky-footer">&copy; WorkOfStan &amp; CRS2 {="All rights reserved."|translate}</footer>
        {if $authUser}{include '../inherite.latte', latte => 'Admin/inc-output-image-selector.latte'}{/if}
    {foreach $clientSideResources['js'] as $jsUri}
        <script type="text/javascript" src="{$jsUri}"></script>
    {/foreach}
        <script n:ifcontent>
            {$inlineJavaScript|noescape}
        </script>
    </body>
</html>
