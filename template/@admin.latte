<!DOCTYPE html>
<html lang="{$language}">
    {*block htmlhead}{/block*}
    {include 'admin-head.latte'}
    <body>
        {*
        <header>
            {include 'navigation.latte'}
        </header>
        <div class="container-fluid row">
            {include 'user-agendas.latte'}
        </div>
        *}
        {block htmlbody}{/block}
        
        {* footer *}
        <footer class="sticky-footer">&copy; WorkOfStan &amp; CRS2 {="All rights reserved."|translate}</footer>
        {* oIS *}
        {if $authUser}{include 'admin-output-image-selector.latte'}{/if}
    {foreach $clientSideResources['js'] as $jsUri}
        <script type="text/javascript" src="{$jsUri}"></script>
    {/foreach}
    <script n:ifcontent>
        {$inlineJavaScript|noescape}
    </script>
    </body>
</html>
